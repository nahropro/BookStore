@model BookStoreModel.ViewModels.ManageAccount.ChangeUserNameViewModel
@{
    ViewBag.Title = "زانیارییه‌كانم (گۆڕینی نازناو)";
}

<p class="page-title">زانیارییه‌كانم</p>

<p class="page-sub-title">گۆڕینی نازناو</p>

@using (Html.BeginForm("ChangeUserName", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        <label for"UserName" class="col-md-2 control-label">نازناوی نوێ</label>
        <div class="col-md-10">
            @Html.EditorFor(m => m.UserName, new { htmlAttributes = new { @class = "form-control" } })
        </div>
    </div>

    <div class="form-group">
        <label for"Password" class="col-md-2 control-label">وشه‌ی نهێنی بۆ دڵنیایی</label>
        <div class="col-md-10">
            @Html.PasswordFor(m => m.PasswordForSecurity, new { @class = "form-control" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button type="submit" class="btn btn-primary">
                <i class="fal fa-save"></i>
                تۆماركردن
            </button>
        </div>
    </div>
}

<div>
    @Html.ActionLink("گه‌ڕانه‌وه‌", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
